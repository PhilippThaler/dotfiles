#!/bin/bash

while true; do
    output=$(fusee-nano ~/bin/hekate.bin 2>&1)
    echo "$output"
    if echo "$output" | grep -q "Smashed the stack"; then
        echo "byyye....."
        break
    fi
    sleep 1s
done
