#!/bin/bash

WINDOW_INSTANCE="scratchpad"

if i3-msg -t get_tree | grep -q "\"instance\":\"$WINDOW_INSTANCE\""; then
    i3-msg "[instance=\"$WINDOW_INSTANCE\"] scratchpad show"
else
    i3-msg "exec --no-startup-id ghostty --x11-instance-name=$WINDOW_INSTANCE"
fi
